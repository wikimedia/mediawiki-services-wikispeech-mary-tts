plugins {
    id 'groovy'
}

dependencies {
    testCompile libs.testng
    testCompile libs.xmlunit
}

test {
    useTestNG()
    systemProperty 'no.userdict', "$rootDir/src/main/dist/user-dictionaries/userdict-no.txt"
}
