#!/usr/bin/env bash

# This doesn't actually test Mary TTS.
# In order to do that we need to execute actual integrations
# that include having the Mishkal service running.
# Mary TTS does however come with self tests that can be excuted
# upon starting the server, but without Mishkal running it will fail.

echo "Test ran successfully"

exit 0
